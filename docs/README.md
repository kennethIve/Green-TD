# Gameplay screenshot

`gameplay.png` could not be uploaded via the GitHub connector (text-only push). Latest F5 capture (orthogonal paths, no tray debug line) was shared with Kenneth / UI UX in DM.

Drop the binary at `docs/gameplay.png` when a write PAT or binary upload path is available — README already links it.
