# Pathing from reference .w3x

Source: `green-circle-td-reference.w3x` (外海量众神 / Green Circle V4.x).

Waypoints taken from `war3map.j` `CreateRegions` rect centers, scaled into 1280×720.

Each corner: spawn → corner → outer ring (CW) → inner ring (CW) → center leak.
